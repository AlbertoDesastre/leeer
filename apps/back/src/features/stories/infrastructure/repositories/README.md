# Repositorios de stories

`InMemoryStoryRepository` es solo un adaptador de ejemplo para que la arquitectura funcione sin conectar una BBDD.

Cuando se implemente Supabase, crea aquí `supabase-story.repository.ts` implementando `StoryRepository`. Los casos de uso no deben cambiar.
