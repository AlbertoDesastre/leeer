{
  "name": "@leeer/back",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deps:install": "pnpm --filter @leeer/back... install",
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "cors": "2.8.5",
    "dotenv": "17.2.1",
    "express": "5.2.1",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/cors": "2.8.19",
    "@types/express": "5.0.3",
    "@types/node": "catalog:",
    "tsx": "4.20.5",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
